import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    padding: 16,
    borderRadius: 12,
    backgroundColor: '#d4d4d8',
    gap: 12,
  },
  title: {
    fontSize: 16,
    fontWeight: 'bold',
  },
})
